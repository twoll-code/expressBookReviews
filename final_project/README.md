# Book Review Application.
A server-side online book review application where ysers can register, log-in, add manage their own reviews for books.
- Uses a secure REST API server, which uses authentication at the session level using JWT.
- Handles common operations with asynchronous operations.